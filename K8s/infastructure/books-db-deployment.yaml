
apiVersion: v1
kind: PersistentVolume
metadata:
  name: books-db-pv
  labels:
    app: books-db
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /var/lib/postgresql/data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: books-db-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: books-db-deployment
  labels:
    app: books-db
spec:
  selector:
    matchLabels:
      app: books-db
  template:
    metadata:
      labels:
        app: books-db
    spec:
      containers:
        - name: books-db
          image: postgres:16
          env:
            - name: POSTGRES_DB
              value: InternshipITexus
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: pgadmin
          ports:
            - containerPort: 5432
              name: postgresql
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: postgresql-persistent-storage
      volumes:
        - name: postgresql-persistent-storage
          persistentVolumeClaim:
            claimName: books-db-pvc
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: books-db-svc
spec:
  ports:
    - port: 5432
  selector:
    app: books-db
  clusterIP: None