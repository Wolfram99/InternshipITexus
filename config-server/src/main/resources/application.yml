spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_URL:https://github.com/Wolfram99/spring-cloud-config}
          clone-on-start: true
        bootstrap: true
      enabled: false
#          label: main
#          default-label:



server:
  port: ${PORT:8888}

